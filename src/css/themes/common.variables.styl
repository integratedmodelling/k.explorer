// App Shared Variables
// --------------------------------------------------
// To customize the look and feel of this app, you can override
// the Stylus variables found in Quasar's source Stylus files. Setting
// variables before Quasar's Stylus will use these variables rather than
// Quasar's default Stylus variable values. Stylus variables specific
// to the themes belong in either the variables.ios.styl or variables.mat.styl files.

// Check documentation for full list of Quasar variables


// App Shared Color Variables
// --------------------------------------------------
// It's highly recommended to change the default colors
// to match your app's branding.

$primary   = #da1f26 //#027be3
$secondary = #26A69A
$tertiary  = #555

$neutral   = #E0E1E2
$positive  = #19a019 //#21BA45
$negative  = #DB2828
$info      = #1e88ce //#31CCEC
$warning   = #F2C037

$modal-border-radius = 20px;
$main-control-width = 512px; // 256px * 2
$main-control-inc-width = 128px; // increment of dimension if needed
$main-control-height = 767px; // header: 59, bottom: 37, gold proportion 833 + 30px of scrollbar margin bottom
$main-control-spc-height = 20px; //height of splitter controllers
$main-control-scrollbar = 30px // scrollbar has a margin bottom of -30px :(
$main-control-header-height = 48px; // calculated value
$main-control-actions-height = 66px // calculated value
$docked-search-height = 52px
$docked-undock-height = 32px
$docked-timeline-height = 34px
$docked-padding = 30px
$docked-correction = 14px

$oi-controls-height = 40px
$oi-metadata-map-wrapper = 100% // only for reference
$oi-metadata-min-height = 50% // metadata
$oi-mapinfo-min-height = 50% // map with selected point and value
$oi-histogram-height = 160px // histogram
$oi-colormap-height = 30px // colormap
$oi-data-details-height = 30px // info
$oi-histogram-minmax-width = 50px // min and max

:root
  --main-control-max-height 90vh
  --q-tree-no-child-min-height 32px

$sem-quality = rgb(0, 204, 0);
$sem-subject = rgb(153, 76, 0);
$sem-identity = rgb(0, 102, 204);
$sem-attribute = rgb(0, 102, 204);
$sem-realm = rgb(0, 102, 204);
$sem-trait = rgb(0, 102, 204);
$sem-event = rgb(153, 153, 0);
$sem-relationship = rgb(210, 170, 0);
$sem-process = rgb(204, 0, 0);
$sem-role = rgb(0, 86, 163);
$sem-configuration = rgb(98, 98, 98);
$sem-domain = rgb(240, 240, 240);
$sem-types = rgb(38, 50, 56);
$sem-preset-observable = rgb(17, 170, 187);
$sem-separator = rgb(10, 10, 10);

$main-control-main-color = rgb(17, 170, 187);
$main-control-green = rgb(231,255,219);
$main-control-cyan = rgb(228,253,255);
$main-control-yellow = rgb(255, 195, 0);
$main-control-red = rgb(255, 100, 100);

$state-forthcoming = $main-control-main-color;
$state-experimental = $warning;
$state-new = $orange-6;
$state-stable = $main-control-main-color;
$state-beta = $warning

// modify stylus variable
$tabs-min-height = 10px;
$tabs-padding = 0 0;
